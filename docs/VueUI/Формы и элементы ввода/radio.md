# Компонент радио-кнопки `ui-radio`

Компонент является составным для компонента `ui-radio-group` и *не может использоваться без него* (вернее может, но в нем тогда не будет вообще никакого логического смысла).
